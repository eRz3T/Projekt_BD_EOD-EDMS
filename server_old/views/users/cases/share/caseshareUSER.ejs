<!DOCTYPE html>
<html lang="pl">
<head>
  <meta charset="UTF-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap">
  <link rel="stylesheet" href="/stylesheets/master.css">
  <title>eObieg - Formularz sprawy</title>
</head>

<body>
  <tytul>eObieg <span style="font-family: 'Times New Roman', Times, serif; font-style: italic; color: darkmagenta; font-weight: lighter;">alfa</span></tytul>

  <tablenav>
    <tbody>
        <tr>
          <form action="/wylogowanie" method="POST">
            <button type="submit" style="font-weight: bolder;">Wyloguj użytkownika</button>
          </form>   
        <td></td>
        <a href="/users/caselist">Powrót do listy spraw</a> 
        <a href="/users/userPanel">Panel użytkownika</a>  
       </tr> 
       <tr><img src="/images/logo.png" alt="Logo" style="height: 50px; width: 50px;"></tr>
    </tbody>
</tablenav>
<srodek>
    <form method="POST">
        <table>
          <thead>
            <tr>
              <th>Wybierz użytkownika</th>
              <th> </th>
            </tr>
          </thead>
          <tbody>
            <tr>
                  <td> <select id="email" name="email">
                    <% for(let i=0; i<emails.length; i++) { %>
                      <option value="<%= emails[i].id %>"><%= emails[i].email %></option>
                    <% } %>
                  </select></td>
                  <td><button type="submit" class="btn btn-primary">Udostępnij</button></td>        
             </tr>  
          </tbody>
        </table>
      </form>
    </srodek>
</body>
</html>


